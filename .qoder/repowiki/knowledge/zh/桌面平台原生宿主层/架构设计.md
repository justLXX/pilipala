- 采用平台隔离架构，每个子目录（linux, macos, windows）独立实现原生引导逻辑。
- Linux/Windows 使用 CMake 构建系统，通过 `flutter/generated_plugin_registrant` 动态注册插件；macOS 使用 CocoaPods 管理依赖并通过 Xcode 项目构建。
- 核心职责包括初始化 Flutter 引擎（`FlutterViewController` / `FlView`）、创建原生窗口以及处理应用生命周期事件。