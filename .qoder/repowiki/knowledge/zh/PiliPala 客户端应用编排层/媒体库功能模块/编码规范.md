- Repository 方法统一返回 ApiResponse<T> 泛型结果，包含 isSuccess 标志和数据/错误信息。
- UseCase 类命名遵循 Get[Feature]UseCase 模式，提供 execute() 方法执行业务逻辑并抛出异常处理错误。
- Controller 继承 GetxController，使用 Rx 类型管理可变状态，通过 getter 暴露不可变视图。
- 异步加载方法采用 try-catch-finally 模式，统一设置 isLoading 和 error 状态。