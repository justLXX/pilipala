- 采用 Clean Architecture 分层结构：data 层（media_repository.dart）封装 API 调用，domain 层（media_use_cases.dart）定义独立用例，presentation 层（media_controller.dart + media_page.dart）处理状态管理和 UI 渲染。
- 依赖方向严格遵循 data → domain → presentation，通过 GetX 的依赖注入（Get.find）解耦各层实例化。
- media.dart 作为 barrel file 统一导出三层公共接口，简化外部引用。