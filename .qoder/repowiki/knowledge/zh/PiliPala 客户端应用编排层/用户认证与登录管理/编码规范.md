- 数据访问层方法统一返回包含 `status` 字段的 `Map<String, dynamic>` 结构，以标准化成功/失败状态的传递。
- 领域层 Use Case 类均遵循单一职责原则，通过构造函数注入 Repository 依赖，并提供统一的 `execute` 入口方法。
- 表现层控制器继承 `GetxController`，利用 `Rx` 响应式变量管理 UI 状态（如加载态、倒计时），并在 `onClose` 中统一释放资源。