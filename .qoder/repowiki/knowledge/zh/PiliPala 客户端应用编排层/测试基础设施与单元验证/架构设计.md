- 分层结构：`helpers/`提供通用数据构造与Widget包装，`mocks/`定义基础服务Mock，`unit/`按业务域（如repository）组织具体用例。
- 依赖隔离：通过手动实现的`TestApiClient`或`mockito`生成的Mock类隔离网络与存储依赖，确保单元测试的纯粹性。
- 入口规范：以`widget_test.dart`为Widget测试入口，各子目录通过`flutter_test`框架执行对应的逻辑断言。