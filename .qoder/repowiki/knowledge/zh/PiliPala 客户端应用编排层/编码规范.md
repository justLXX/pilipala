- 路由定义集中在 `lib/router/app_pages.dart`，并通过 `CustomGetPage` 统一过渡与全屏配置。
- 依赖注入在 `lib/core/di/dependency_injection.dart` 集中声明，并按 Feature 分组注册 Repository/UseCase/Controller。
- 路由级依赖通过 `lib/router/bindings.dart` 中的 `Bindings` 子类按需提供，与全局 DI 形成互补。
- 新重构的 Feature 页面通过 `as` 别名导入以避免命名冲突，并在路由中优先使用新版本。