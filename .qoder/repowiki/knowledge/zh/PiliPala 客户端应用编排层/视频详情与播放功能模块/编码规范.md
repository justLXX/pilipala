- 领域层 UseCase 类统一命名为 `XxxUseCase` 并提供 `execute` 方法作为唯一入口。
- 数据层 Repository 方法统一返回 `ApiResponse<T>` 泛型结果，封装成功与错误状态。
- 表现层 Controller 继承 `GetxController`，使用 `Rx` 类型定义私有状态变量，并通过 getter 暴露公开状态。
- 页面组件中复杂 UI 区块（如操作栏、UP主信息）抽取为私有 Widget 类（以 `_` 开头），保持主页面代码简洁。