- 网络层采用统一响应包装模式，所有 API 返回标准化的 `ApiResponse<T>` 对象以隔离业务逻辑与底层错误。
- 依赖注入广泛使用懒加载策略（`Get.lazyPut`），并在路由绑定（`Bindings`）中按功能模块隔离依赖生命周期。
- 核心服务提供抽象接口（如 `ApiClient`、`StorageService`），具体实现通过工厂类或 DI 容器注入，以支持测试与解耦。