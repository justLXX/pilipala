- 采用 GetX 进行状态管理，`MainController` 继承 `GetxController` 维护导航状态与用户信息。
- 视图层 `MainApp` 使用 `PageView` 实现无滑动切换，结合 `NavigationBar` 或 `BottomNavigationBar` 渲染底部菜单。
- 通过 `main.dart` 统一导出表现层组件，作为该功能模块的对外接口。
- 依赖 `Hive` 进行本地配置（如导航排序、隐藏设置）的持久化读取。