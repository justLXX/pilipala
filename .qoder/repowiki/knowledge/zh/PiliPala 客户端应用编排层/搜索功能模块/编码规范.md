- 用例类（UseCase）通过构造函数接收依赖，若未提供则使用 Get.find() 从服务定位器获取默认实例。
- Repository 方法统一返回泛型 ApiResponse<T>，在 Repository 层处理成功/失败状态转换，而非在用例或控制器中处理原始 HTTP 响应。
- 控制器中的异步操作遵循“设置加载状态 -> try/catch 执行业务逻辑 -> finally 重置加载状态”的标准模式，并将异常信息存入响应式错误状态。