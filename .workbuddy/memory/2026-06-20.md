# 2026-06-20

- 修复播放器与全屏 loading 遮挡状态栏/返回按钮的问题：
  - 在 `lib/plugin/pl_player/view.dart` 的 loading 层使用 `IgnorePointer` 避免拦截控制栏点击，同时用 `Positioned.fill` + `SafeArea` 在左上角提供独立返回按钮，全屏时退出全屏、非全屏时返回上一页。
  - 在 `lib/features/video/presentation/video_detail_page.dart` 的页面级 loading/error 状态添加 `_buildTopBackButton()`，确保网络卡住时也能退出。
  - 将所有 `SmartDialog.showLoading()` 调用增加 `clickMaskDismiss: true` 与 `backType: SmartBackType.normal`，允许点击遮罩或返回键关闭 loading。
